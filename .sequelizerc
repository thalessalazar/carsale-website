const { resolve } = require("path");

module.exports = {
    "config": resolve(__dirname, "app", "config", "database.js"),
    "models-path": resolve(__dirname, "app", "models"),
    "migrations-path": resolve(__dirname, "app", "database", "migrations"),
    "seeders-path": resolve(__dirname, "app", "database", "seeders")
}